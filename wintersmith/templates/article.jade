
extends layout

block prepend title
  | #{ page.title } -


block content
  include author
  a.back(href='/') ← back to index
  h1= page.title
  p
    | #{ page.metadata.date }
    | written by:
    mixin author(page.metadata.author)
  article.post
    section.content!= page.html

  include disqus